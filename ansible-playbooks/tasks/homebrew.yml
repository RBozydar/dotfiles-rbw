---
- name: Check if Homebrew is installed
  ansible.builtin.stat:
    path: "{{ homebrew_bin }}"
  register: brew_installed

- name: Install Homebrew
  when: not brew_installed.stat.exists
  become: "{{ user_task_become | bool }}"
  become_user: "{{ username }}"
  ansible.builtin.shell:
    cmd: /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
  environment:
    NONINTERACTIVE: "1"

- name: Install Homebrew formulae
  become: "{{ user_task_become | bool }}"
  become_user: "{{ username }}"
  community.general.homebrew:
    name: "{{ brew_packages }}"
    state: present
  environment:
    PATH: "{{ homebrew_path_env }}"

- name: Install Homebrew casks
  become: "{{ user_task_become | bool }}"
  become_user: "{{ username }}"
  community.general.homebrew_cask:
    name: "{{ brew_casks }}"
    state: present
    accept_external_apps: true
  when: enable_gui | bool
  environment:
    PATH: "{{ homebrew_path_env }}"

- name: Register yazi piper plugin
  become: "{{ user_task_become | bool }}"
  become_user: "{{ username }}"
  ansible.builtin.command:
    cmd: "{{ yazi_bin }} pkg add yazi-rs/plugins:piper"
  register: yazi_plugin_result
  changed_when: "'already exists' not in yazi_plugin_result.stderr"
  failed_when: "yazi_plugin_result.rc != 0 and 'already exists' not in yazi_plugin_result.stderr"

- name: Install yazi plugins
  become: "{{ user_task_become | bool }}"
  become_user: "{{ username }}"
  ansible.builtin.command:
    cmd: "{{ yazi_bin }} pkg install"
  changed_when: true
