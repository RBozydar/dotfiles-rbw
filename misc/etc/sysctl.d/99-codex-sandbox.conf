# Allow bubblewrap-based sandboxes to create unprivileged user namespaces.
kernel.unprivileged_userns_clone=1

# Required on Ubuntu/AppArmor hosts where user namespaces are restricted.
kernel.apparmor_restrict_unprivileged_userns=0
